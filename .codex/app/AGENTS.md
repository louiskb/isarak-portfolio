# App Folder Rules

- Keep request handling in controllers; move non-trivial logic out when it grows.
- Keep model logic explicit and readable.
- Keep views light; extract helpers/partials when templates become complex.
- Follow strong params and explicit permitted attributes.
- Use Bootstrap conventions consistently; avoid ad-hoc inline styles.
