# CI Rules

- Preserve existing CI intent: security scans, lint, tests, and system tests.
- Keep workflow edits minimal and explain expected impact.
- Prefer official maintained actions.
- Never add plaintext secrets to workflow files.
- Keep CI commands aligned with local `bin/*` scripts.
