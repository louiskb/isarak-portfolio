# Test Folder Rules

- Prefer behavior-focused tests over implementation detail tests.
- Add regression tests for bug fixes when feasible.
- Keep tests deterministic and scoped.
- During iteration, run targeted tests first, then full relevant suites.
