<% content_for :title, "Edit — #{@grant_award.title}" %>

<div class="container py-5">
  <div class="row justify-content-center">
    <div class="col-lg-8">
      <nav aria-label="breadcrumb" class="mb-4">
        <ol class="breadcrumb">
          <li class="breadcrumb-item"><%= link_to "Grants & Awards", grant_awards_path %></li>
          <li class="breadcrumb-item"><%= link_to @grant_award.title, @grant_award %></li>
          <li class="breadcrumb-item active" aria-current="page">Edit</li>
        </ol>
      </nav>

      <h1 class="mb-2">Edit grant or award</h1>
      <p class="text-muted mb-4"><%= @grant_award.title %></p>

      <%= render "form", grant_award: @grant_award %>

    </div>
  </div>
</div>
