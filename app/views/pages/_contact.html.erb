<%# Contact section — anchored for navbar "Contact Me" CTA link %>
<section id="contact" class="home-contact">
  <div class="container">
    <div class="row justify-content-center">
      <div class="col-lg-8">
        <p class="home-section-label">Contact</p>
        <h2 class="home-section-title mb-3">Get in Touch</h2>
        <p class="home-contact-sub mb-5">
          Interested in collaborating on research or have a question? Fill out the form below
          or email me directly at
          <%= link_to "isara@yourdomain.com", "mailto:isara@yourdomain.com", class: "home-contact-email-link" %>.
        </p>

        <%# Contact form %>
        <%= simple_form_for @contact, url: contacts_path, local: true, data: { controller: "load-button" }, class: "mb-2" do |f| %>
          <div class="row">
            <div class="col-12 col-md-6">
              <%= f.input :first_name, label: "First Name" %>
            </div>
            <div class="col-12 col-md-6">
              <%= f.input :last_name, label: "Last Name" %>
            </div>
          </div>
          <%= f.input :email, label: "Your Email" %>
          <%= f.input :message, as: :text, rows: 6, label: "Message" %>
          <%# Honeypot spam protection %>
          <%= invisible_captcha %>
          <%= f.button :submit, "Send Message",
              data: { action: "click->load-button#loader" },
              class: "btn home-btn-primary mt-4 mb-4" %>
        <% end %>
      </div>
    </div>
  </div>
</section>
