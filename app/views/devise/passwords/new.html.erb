<div class="container py-5">
  <div class="row justify-content-center">
    <div class="col-md-6 col-lg-5">
      <div class="card border-0 shadow-sm">
        <div class="card-body p-4 p-md-5">
          <h2 class="card-title text-primary mb-1">Forgot your password?</h2>
          <p class="text-muted mb-4">Enter your email and we'll send you reset instructions.</p>

          <%= simple_form_for(resource, as: resource_name, url: password_path(resource_name),
                              html: { method: :post, data: { controller: "load-button" } }) do |f| %>
            <%= f.error_notification %>

            <%= f.input :email,
                        required: true,
                        autofocus: true,
                        input_html: { autocomplete: "email" } %>

            <div data-load-button-target="buttons">
              <%= f.button :submit, "Send reset instructions", class: "btn btn-primary w-100 mt-2",
                           data: { action: "click->load-button#showSpinner",
                                   load_button_loading_text_value: "Sending instructions..." } %>
            </div>
          <% end %>
        </div>
      </div>

      <%= render "devise/shared/links" %>
    </div>
  </div>
</div>
