# Future Improvements — isarak-portfolio

This file tracks features and enhancements to suggest to Isara in future builds.
Add to this list as we continue developing the site.

---

## Blog

- **Category / tag filtering on blog index** — Add a tag or category field to `BlogPost`. On the index page, render filter pills (e.g. "All", "Research", "Teaching", "Opinion") that filter the visible posts. Consider using Stimulus + CSS show/hide for a no-reload feel, or a simple query param approach.

---

## General

*(Add more here as we build)*
