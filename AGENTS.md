# Codex Project Pointer (Non-Claude)

This file exists only to route Codex context.

For project instructions, use:
- `./.codex/AGENTS.md`
- folder overrides under `./.codex/`

Do not read `.claude/` files as Codex instruction sources.
Do not create or modify `.claude/` files unless explicitly asked.
